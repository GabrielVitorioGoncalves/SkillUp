<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Skill UP - Criar Curso</title>

    <link rel='stylesheet' href='/stylesheets/sidebarAdmin.css'>
    <link rel='stylesheet' href='../../stylesheets/temas.css'> </head>

<body>
    <div class="page-wrapper">
        <%- include('partials/admin/sidebar') %>

        <main class="main-content">
            <div class="form-container">
                <div class="form-card">
                    <h1 class="form-title">Criar Novo Curso</h1>
                    <p class="form-subtitle">Preencha os dados principais abaixo. Após salvar, você será redirecionado para adicionar as aulas.</p>

                    <form action="/admin/cria-curso" method="POST" enctype="multipart/form-data">

                        <div class="form-group">
                            <label for="nome">Nome do Curso</label>
                            <input type="text" id="nome" name="nome" placeholder="Ex: Curso Completo de JavaScript" required>
                        </div>

                        <div class="form-group">
                            <label for="descricao">Descrição</label>
                            <textarea id="descricao" name="descricao" rows="5" placeholder="Descreva o que os alunos aprenderão neste curso..." required></textarea>
                        </div>

                        <div class="form-group">
                            <label>Categorias (pode marcar várias)</label>
                            <div class="checkbox-group">
                                <% if (locals.categorias && categorias.length > 0) { %>
                                    <% categorias.forEach(cat => { %>
                                        <div class="checkbox-item">
                                            <input type="checkbox" name="categorias" id="cat-<%= cat.id_tema %>" value="<%= cat.id_tema %>">
                                            <label for="cat-<%= cat.id_tema %>"><%= cat.cat_nome %></label>
                                        </div>
                                    <% }); %>
                                <% } else { %>
                                    <p style="color: #a0a0a0;">Nenhuma categoria encontrada. Crie uma na seção 'Categorias' primeiro.</p>
                                <% } %>
                            </div>
                        </div>

                        <div class="form-group">
                            <label for="capa">Capa do Curso (opcional)</label>
                            <input type="file" id="capa" name="capa" accept="image/*">
                        </div>

                        <div class="form-actions">
                            <button type="submit" class="btn btn-primary">Criar Curso e Adicionar Aulas</button>
                        </div>
                    </form>
                </div>
            </div>
        </main>
    </div>
</body>
</html>